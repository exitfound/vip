complete -C {{ local_vault_manual_cluster_path_binary }}/vault vault
export VAULT_ADDR="{{ (global_vault_cluster_enable_https | bool) | ternary('https', 'http') }}://127.0.0.1:{{ global_vault_host_listening_port }}"
