---
# Переменные, необходимые для установки и настройки Transit Single Vault с помощью Docker, применимые ко всем дистрибутивам в равной степени:
local_vault_docker_transit_dir_mode: "0755"
local_vault_docker_transit_dir_main: "/opt/vault"
local_vault_docker_transit_dir_data: "/opt/vault/data"
local_vault_docker_transit_dir_config: "/opt/vault/config"
local_vault_docker_transit_file_mode: "0644"
local_vault_docker_transit_file_config: "config.hcl"
local_vault_docker_transit_file_composefile: "vault-docker-compose.yaml"
local_vault_docker_transit_file_dockerfile: "vault.dockerfile"

# Переменные, необходимые для установки и настройки Transit Single Vault с помощью Docker на дистрибутиве семейства SUSE:

# Переменные, необходимые для установки и настройки Transit Single Vault с помощью Docker на дистрибутиве семейства RedHat:

# Переменные, необходимые для установки и настройки Transit Single Vault с помощью Docker на дистрибутиве семейства Debian:
